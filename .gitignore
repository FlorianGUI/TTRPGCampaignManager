.env
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.idea/
dist/
.coverage
htmlcov/
# Installed dependencies. Never tracked — a symlink or a directory here is a
# machine's own layout, and one committed by accident breaks `npm ci` for
# everyone who clones (it happened: see the commit that removed it).
node_modules/
